<%= form_with(model: deposit) do |form| %>
  <% if deposit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(deposit.errors.count, "error") %> prohibited this deposit from being saved:</h2>

      <ul>
        <% deposit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Valor", style: "display: block" %>
    <%= form.number_field :amount, class:"form-control" %>
  </div>

  <%= form.number_field :account_id, id: :account_id, class:"form-control", value: current_account.id, type: :hidden  %>
  
  <div>
    <%= form.submit "Realizar Deposito", class:"btn btn-primary mt-3" %>
  </div>
<% end %>
